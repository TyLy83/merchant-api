import IModel from "./model.interface";

interface IReposity {
    
    table:string;
    // createRecord(model: IModel): Promise<IModel>;
    // findRecord(id: number): Promise<IModel>;
    // updateRecord(model: IModel): Promise<IModel>;
    // deleteRecord(id: number): Promise<IModel>;

}

export default IReposity;